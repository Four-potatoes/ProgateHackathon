# React 앱 추가 패키지 설치

다음 명령어를 실행하세요:

```bash
cd client

npm install axios react-router-dom
npm install -D @types/react-router-dom

# Tailwind CSS 설치
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

설치가 완료되면 제가 제공하는 파일들을 복사하시면 됩니다!
